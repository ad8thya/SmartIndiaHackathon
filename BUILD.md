# BUILD LOG — what you have inherited

> **README.md** answers *"how do I run this and what do I build?"*
> **This file** answers *"what is actually here, what is fake, and what has already been decided?"*
>
> Ten minute read. Tables, not prose.

---

## 1. What this is

URBAN TWIN turns Chennai's existing MTC bus fleet into a mobile sensor network. Buses
already drive every road in the city on a fixed schedule; bolting a camera and an edge
box onto them produces a continuously-updating survey — road surface distress classified
to IRC:82-2015, traffic density from probe vehicles, pedestrian conflict zones near
schools, and hit-and-run incidents with plate evidence. Detections from many buses are
*fused*: three different vehicles seeing the same pothole is evidence, one bus seeing it
thirty times is a dirty lens. Only corroborated events escalate to a municipal work order
with an SLA clock. The output is a 3D command centre plus a mobile field app for repair
crews.

**The architecture is mock-first.** Every module ships a working fake that produces
convincing synthetic data today; the real implementation is a stub that raises
`NotImplementedError`; a per-module `USE_REAL_*` environment flag chooses between them at
runtime through a factory function. That is the whole reason six people can work in
parallel — nobody imports anybody's implementation, only their factory, so swapping a
mock for real code is a one-line change inside one folder that no other file observes.

---

## 2. Current state — honest inventory

> ### ⚠ THE SYSTEM CURRENTLY RUNS ENTIRELY ON SYNTHETIC DATA.
> **There is no real detection, no real OCR, and no real graph routing.**
> Every detection on the screen is generated by a mock. The demo is genuine
> end-to-end plumbing driving fabricated inputs. Do not discover this on day 3.
>
> What is **not** synthetic, and can be said out loud: the map (real OSM vector
> tiles and real building footprints), the fusion maths (genuine noisy-OR and a
> real status ladder), the urban risk index (a real transparent weighted model),
> and every number the what-if engine returns is *derived* from the seeded
> network rather than typed in — see the whatif row below.

| MODULE | OWNER | MOCK STATUS | REAL STATUS | FILE TO EDIT |
|---|---|---|---|---|
| **defects** | M1 | **Working.** Fires at 14 fixed hotspots within 70 m of a passing bus; confidence climbs across repeat passes; IRC severity from the hotspot with occasional one-class drift; 0.8 % chance/frame of an unscripted novel defect; silent below 3 km/h | **Stub** — `detect()` raises `NotImplementedError`. 3 stubs total | `services/edge/defects/impl.py` |
| **defects — ZEBRA_CROSSING severity** | M1 | **ZEBRA_CROSSING severity is a fixed placeholder, not measured; condition scoring is future work.** Always `ZEBRA_CROSSING_PLACEHOLDER_SEVERITY` (`MEDIUM`) — never drifted, never rolled, unlike every other class in this row | **No mechanism exists or is scheduled.** Real detection needs the same placeholder (or a real condition-scoring step) until it's built | `services/edge/defects/mock.py` |
| **pedestrian** | M3 | **Working.** 3 seeded school zones; inside one, 1–3 sightings/frame and a 35 % base risk rate that climbs with speed over the 25 km/h zone limit; outside, ~6 % plain sightings and never a risk | **Stub** — `detect()` raises. 2 stubs | `services/edge/pedestrian/impl.py` |
| **incidents** | M4 | **Working.** One scripted hit-and-run per replay loop: bus `MTC-VYASARPADI-3311`, segment `SEG-21G-002`, plate `TN 09 BX 4412`, OCR confidence `0.87`, 4 evidence frames. Plus ~1.2 %/frame rash-driving, ~35 % of which have unreadable plates | **Stub** — `process()` raises. 3 stubs | `services/edge/incidents/impl.py` |
| **traffic** | M2 | **Working.** Twin-peaked congestion by hour-of-day, per-corridor phase shift, modulated by observed VEHICLE counts; PCI degrades from M1's defects; Greenshields speed. Clock comes from the newest observation, not wall time | **Stub** — `analyze()` raises. 2 stubs | `services/cloud/intelligence/traffic_analytics/impl.py` |
| **whatif** | M2 | **Working, and derived rather than hardcoded.** Every one of the 26 segments gets its own delta, computed from segment length, routes sharing the corridor, lanes, free-flow speed and whether a parallel road exists. Deterministic (SHA-256 of the segment id, not `random`); no two roads return the same answer; the three rehearsed headline closures are pinned. **A heuristic, not graph routing** — the docstring says so | **Stub** — `simulate()` raises. 2 stubs | `services/cloud/intelligence/whatif/impl.py` |
| **fusion** | M3 | **Working, and largely real.** Genuine noisy-OR confidence, genuine status ladder, stable event ids, confidence-weighted centroids, worst-severity-wins, SLA clocks, `FUSABLE_CLASSES` filtering. Only the *clustering* is faked: snap-to-25 m-grid instead of DBSCAN | **Stub** — `fuse()` raises. `_fusable()` helper is real and already correct | `services/cloud/consensus/impl.py` |
| **risk** | M3 | **Working, and the intended v1.** A real, transparent 6-component weighted index (PCI 30% / congestion 20% / pedestrian density 15% / school proximity 15% / near-miss frequency 12% / recent incidents 8%). `components` always sum to `score`; `explanation` never empty | **Stub** — `score()` raises. Learned (gradient-boosted) upgrade path documented, same explainability requirement | `services/cloud/intelligence/urban_risk/impl.py` |
| **recommend** | M2 | **Working.** Five deterministic proximity rules off the same `RiskContext` risk reads (ZEBRA_CROSSING, SIGNAL_TIMING, DIVIDER, DRAINAGE, SPEED_CALMING) | **Stub** — `recommend()` raises. Real evidence-id lookup from postgres documented | `services/cloud/intelligence/recommend/impl.py` |
| **api** | M5 | n/a — real | **Real.** 19 HTTP routes + 1 WebSocket. Degrades to in-memory cache when postgres is down | `services/cloud/api/**` |
| **db** | M5 | n/a — real | **Real.** 9 tables, PostGIS Geography, 1 migration, autogenerate verified empty | `packages/db/**` |
| **contracts** | shared | n/a | **Real, and FROZEN (v1.1.0).** 8 Protocols, 17 models, pure fusion maths. One approved one-time amendment applied — see §10 | `packages/contracts/**` (team decision) |
| **frontend** | M6 | n/a — real | **Real.** ONE app (`apps/web`) on ONE port: role picker, all 8 role views, and the field app at `/field`. Real offline basemap (committed PMTiles), real 3D buildings, contract types generated from `packages/contracts` | `apps/web/src/**` |

### What the mocks actually produce

| | |
|---|---|
| Routes | 6 — `27B`, `42A`, `51C`, `21G`, `570`, `M1` |
| Road segments | 26 (`SEG-<route>-<nnn>`) |
| Buses | 6, one per route, spread along their routes |
| School zones | 3 |
| Defect hotspots | 14 |
| Near-miss junctions | **10**, across all six routes. TTC 0.4–1.4 s, all distinct; severity derived from TTC, never hand-set |
| Seeded events | 41, spread across all 9 workflow states |
| 3D buildings | 7,177 **real OSM** footprints, 1.72 MB, heights 3–172 m |
| Basemap | **Real vector tiles.** Protomaps extract of greater Chennai, 17 MB, committed. No API key, no CDN, no tile server — renders with the network off |

### Every module has the identical shape

```
services/<module>/
  README.md         what it does, its Protocol, how to run it standalone
  config.py         module settings + the USE_REAL_* flag
  mock.py           the working fake — the demo runs on this
  impl.py           your real work goes here (stubbed, with a numbered TODO list)
  factory.py        get_x() → mock or impl
  test_module.py    tests written against the PROTOCOL, so they pass against both
```

---

## 3. Architecture

```
   ┌──────────────┐
   │  MTC BUSES   │  6 simulated · AIS-140 telemetry · forward camera
   └──────┬───────┘
          │ frames + GPS
          ▼
   ┌─────────────────────────────────────────────────────┐
   │  EDGE PERCEPTION                                     │
   │  ┌───────────┐ ┌────────────┐ ┌──────────────────┐   │
   │  │ M1 Defects│ │M3 Pedestr. │ │ M4 Incidents+ANPR│   │
   │  └───────────┘ └────────────┘ └──────────────────┘   │
   └───────────────────────┬─────────────────────────────┘
                           │  Observation · IncidentReport
                           ▼
                  ┌──────────────────┐
                  │   MQTT (1883)    │  bus/{id}/position
                  │   mosquitto      │  bus/{id}/observation
                  └────────┬─────────┘  bus/{id}/incident
                           ▼
   ┌─────────────────────────────────────────────────────┐
   │  M5 PLATFORM                                        │
   │   MqttBridge → LiveState → FusionLoop (every 4 s)   │
   │                               │                     │
   │   ┌───────────────────────────┴──────────────┐      │
   │   │  M3 EventFuser — cluster + noisy-OR +    │      │
   │   │  derive_status: DETECTED → AI_VERIFIED → │      │
   │   │  AUTHORITY_NOTIFIED                      │      │
   │   └───────────────────────────┬──────────────┘      │
   │                               ▼                     │
   │   PostGIS (Geography 4326) · Redis · FastAPI        │
   │   M2 TrafficAnalyzer  ·  M2 WhatIfEngine            │
   └───────────┬──────────────────────────┬──────────────┘
         REST  │                          │  WS /ws/live
               ▼                          ▼
   ┌───────────────────────┐   ┌──────────────────────┐
   │  M6 COMMAND CENTRE    │   │  M6 FIELD APP        │
   │  MapLibre + deck.gl   │   │  Feed · Detail ·     │
   │  :5173                │   │  Map · MyTasks :5174 │
   └───────────────────────┘   └──────────────────────┘
```

### The four things you must understand

**1 — `packages/contracts` is frozen.** Everything imports it; nothing in it imports
anything else. Adding an optional field or an enum member is cheap. A rename or removal
breaks five people at once. Changing it requires an ACK from every affected owner, a full
`make test`, and `CONTRACTS_OK=1 git commit` (a pre-commit hook enforces the pause).

**2 — the eight Protocols ARE the module boundaries.** They are structural
(`@runtime_checkable`), so you do **not** subclass them — write a plain class with
matching methods. The last two were added in the §10 contracts amendment.

| Protocol | Owner | Method |
|---|---|---|
| `DefectDetector` | M1 | `detect(frame, meta) -> list[Observation]` |
| `PedestrianRiskDetector` | M3 | `detect(frame, meta) -> list[Observation]` |
| `IncidentDetector` | M4 | `process(frames, meta) -> list[IncidentReport]` |
| `TrafficAnalyzer` | M2 | `analyze(observations) -> dict[str, RoadCondition]` |
| `EventFuser` | M3 | `fuse(observations) -> list[Event]` |
| `WhatIfEngine` | M2 | `simulate(req) -> list[WhatIfResult]` |
| `RiskScorer` | M3 | `score(road_id, ctx) -> UrbanRiskScore` |
| `RecommendationEngine` | M2 | `recommend(road_id, ctx) -> list[InfrastructureRecommendation]` |

**3 — the `USE_REAL_*` flags.** Eight of them, all `false` in `.env.example`.
`USE_REAL_DEFECTS`, `USE_REAL_PEDESTRIAN`, `USE_REAL_INCIDENTS`, `USE_REAL_TRAFFIC`,
`USE_REAL_FUSION`, `USE_REAL_WHATIF`, `USE_REAL_RISK`, `USE_REAL_RECOMMEND`. **Flip yours
only when your own tests pass.** It is your flag in your `.env` (gitignored) — nobody
else's demo changes either way. CI fails the build if a `USE_REAL_*` is ever `true` in
`.env.example`.

**4 — the factory pattern.** Real code, `services/edge/defects/factory.py`:

```python
@lru_cache(maxsize=1)
def get_defect_detector() -> DefectDetector:
    settings = get_settings()
    if settings.USE_REAL_DEFECTS:
        return RealDefectDetector(settings)
    return MockDefectDetector(settings)
```

Every consumer calls `get_defect_detector()`. Nobody imports `MockDefectDetector` or
`RealDefectDetector` directly — which is exactly why M1 can swap implementations without
a single other file changing. The replay simulator calls the *factories*, so the moment
you flip your flag your real code is in the live loop.

---

## 4. What was verified

A 34-point verification pass ran against live infrastructure — real containers, real
PostGIS, real MQTT roundtrips, and headless Chrome driving the actual UI over CDP.
**28 of 34 passed; 6 failed; all were fixed.**

Highlights of what passed on the first attempt: all six Protocols and their signatures;
every mock *and* every stub satisfying its Protocol both at runtime and under `mypy`;
`fuse_confidence` and `derive_status` at their exact boundaries (0.70, 0.95, 2 vs 3
buses); all 12 `Observation` rejection cases; GIST indexes on all 6 geom columns;
`ST_DWithin` confirmed returning metres; all 15 endpoints returning 200 with 404s (not
500s) on unknown ids; the WebSocket pushing within 10 s; both apps building.

### Fix log

| ID | What was broken | What changed | Files |
|---|---|---|---|
| **F1** | `docker compose up` died with `6379 address already in use` on any machine with a local Redis. Postgres' port was env-overridable; redis/mqtt/api were hardcoded | All 5 host ports are now `${VAR:-default}` | `docker-compose.yml`, `.env.example` |
| **F2** | The `[ml]` extra installed `opencv-python`, `opencv-contrib-python` *and* `opencv-python-headless` — three distributions writing the same `cv2` module | Dropped the explicit headless pin; ultralytics/paddleocr bring their own | `pyproject.toml` |
| **F3** | `mypy packages/` failed on an unused `# type: ignore`. Passed only because the Makefile narrowed the path | Removed it | `packages/contracts/test_contracts.py` |
| **F4** | `alembic revision --autogenerate` produced **39 spurious operations** instead of an empty migration — 23 `server_default`s in the migration absent from the ORM, 10 nullable timestamps, 1 double-prefixed check constraint | Migration 0001 now mirrors the ORM exactly. Literal `server_default`s: **0**; `func.now()` on timestamps: **10** (declared in both) | `0001_initial_postgis_schema.py` |
| **F5** | `uq_event_obs` was declared on the ORM but absent from the DB — it duplicated the composite primary key | Removed from ORM and migration | `packages/db/src/db/models.py`, migration 0001 |
| **F6** | *(declined — see §5)* | | |
| **F7** | `buildings.geojson` was a synthetic block grid (every ring exactly 5 vertices), and the fetch script covered a hardcoded box | BBox derived from `citydata.ROUTES` + 500 m; `--tagged-only` filter; heights floored at 3 m and rounded; coords to 6 dp | `scripts/fetch_buildings.py`, `Makefile` |
| **F8** | *(no action — pytest warning noise only)* | | |
| **F9** | Plain `PEDESTRIAN` sightings were fused into workflow Events with a fabricated `SMALL` severity and a 720-hour SLA. **83 of 149 events were pedestrians**; the field app read *"183 open across your zone"* and offered crews `pedestrian … SLA 30d` | Added `FUSABLE_CLASSES` to contracts (the only authorised contract change); both `mock.py` and `impl.py` filter on it. `_worst_severity` now returns `Severity \| None` and never invents one; safety classes resolve through an explicit `_SAFETY_SEVERITY` table; an infrastructure class without severity **raises**. PEDESTRIAN events: **83 → 0** | `contracts/enums.py`, `contracts/__init__.py`, `services/cloud/consensus/mock.py`, `services/cloud/consensus/impl.py` |
| **F10** | `.gitignore` missed `*.pt`, `*.pth`, `*.db`, `data/raw_video/`, `data/processed/` | Added those plus `*.onnx`, `*.pdparams`, `models/`, `data/*.pkl`, `*.sqlite3` | `.gitignore` |
| **F11** | Every `make revision` ended in `FAILED: Could not find entrypoint console_scripts.ruff` — ruff ships as a binary, not a Python entrypoint | Removed the post-write hook; generated migrations are ruff-excluded anyway | `packages/db/alembic.ini` |
| **F12** | `/api/analytics/summary` counted only in-memory events while `/api/events` merged postgres + memory. The KPI strip read **9** while the panel below it showed **53** | Extracted `merged_events()` as the single definition of "all events"; both endpoints call it. Verified live: **37 vs 37** | `services/cloud/api/routers/events.py`, `services/cloud/api/routers/analytics.py` |

There is no F13 or F14. The remaining findings were deliberately deferred — see §5.

Tests grew **168 → 177** across these fixes. Every fix has a regression test.

---

## 5. Known gaps — deliberately not fixed

**These are annotated in code, not silently ignored.** Find them with:

```bash
grep -rn "TODO (M5)" services/cloud/api
```

Each also appears in the relevant member's 7-day checklist in README.md.

The root cause is narrow: **only `events` and `work_orders` are written at runtime.**
`observations`, `incidents` and `bus_positions` exist in the schema, are indexed, and are
written *only by `scripts/seed.py`*. The MQTT bridge puts them in memory and nowhere else.

| File | Symptom | Owner | Why deferred |
|---|---|---|---|
| `services/cloud/api/routers/fleet.py:13` | Reads `LiveState` only. With no replay running, `/api/fleet` returns `[]` despite 6 seeded `bus_positions` — the map opens with no buses | M5 | Needs the persistence decision below; not a one-line patch |
| `services/cloud/api/routers/incidents.py:24` | The `incidents` table is never written; every dossier is lost on API restart | M5 + M4 | M4 owns the evidence chain — whether incidents persist is their call, not a scaffold repair |
| `services/cloud/api/routers/analytics.py:35` | `incidents_today` really means "since this process started" | M5 | Same dependency as above |
| `services/cloud/api/routers/analytics.py:64` | `km_surveyed_today` and `buses_online` are process-lifetime counters; `km_surveyed` resets on every `uvicorn --reload` | M5 | Needs redis-backed or `bus_positions`-derived aggregation — a design choice |
| `services/cloud/api/routers/ws.py:58` | The WS `HELLO` payload bypasses `merged_events()`, so a fresh browser against a fresh API sees an empty map until the REST bootstrap lands | M5 | Harmless today (`App.tsx` calls `bootstrap()` alongside `connect()`), but it is the same drift class as F12 |
| `services/cloud/api/routers/events.py:144` | `get_event` hand-rolls the memory-then-postgres lookup instead of calling `merged_events()` | M5 | Cosmetic; behaviour is already correct |
| `services/cloud/api/mqtt_bridge.py:103` | Observations land in memory only. **M2 cannot span an API restart** with `TRAFFIC_WINDOW_MINUTES`, and `event_observations` has nothing to point at | M5 (M2 blocked) | Needs batched inserts — at 6 buses × 15 fps a per-message round trip will not keep up. A day-1 platform decision |

### ~~The frontend contract is hand-mirrored three times~~ — fixed

**Resolved in the consolidation build.** The three Vite apps (`command`,
`field`, `roles`) are now one app, `apps/web`, and its contract types are
**generated** from `packages/contracts` by `scripts/gen_frontend_types.py`
(`make types`). It introspects the actual pydantic models and StrEnums, so a
rename in `enums.py` cannot silently leave the frontend behind the way
`MISSING_SIGN` / `FADED_ZEBRA` did. `apps/web/src/lib/types.ts` carries a
do-not-edit header; the generator is deterministic, so CI can diff it.

`src/field/lib/api.ts` and `src/roles/lib/api.ts` still exist but hold only
presentation helpers (chip colours, `timeAgo`) over the one shared client —
no contract types are mirrored anywhere any more.

### Notebook lint findings, deliberately not fixed

`notebooks/` is excluded from ruff (see `pyproject.toml` for why: 117
findings, ~95% E501/E402/I001, and a permanently red CI teaches people to
ignore CI). Six of those findings were **not** style noise and are recorded
here so they stay visible to their owner rather than being silenced:

| File | Finding | Note |
|---|---|---|
| `02_train_road_damage.ipynb` | `contact_sheet` used, never imported (2×) | `notebooks/common/contact_sheet.py` exists — the cell is missing `from common import contact_sheet`. Will raise `NameError` on first run |
| `02_train_road_damage.ipynb` | `data_splits` used, never defined (3×) | Feeds the model card's `dataset_size`. `notebooks/common/splits.py` exists; what should assign `data_splits` is M1's call |
| `03_prepare_hazards.ipynb` | `shutil` imported, unused | Harmless |

None of these notebooks has ever been executed, so none of this is a
regression — but the first run of notebook 02 will fail until they are fixed.

### Declined, with reasoning

| Item | Decision | Why |
|---|---|---|
| Bump Vite to clear the `esbuild` advisory (`GHSA-67mh-4wv8-2f99`) | **Declined** | Dev-server-only: it lets a website read responses from a *running dev server*. It does not affect build output or anything deployed. A major bundler upgrade on day 3 of 7 is the larger risk. Mitigation: dev servers bind to the LAN (`--host`, needed for phone testing), so do not run `make dev` on untrusted wifi. Documented in README troubleshooting |
| Rename `contracts` / `db` / `citydata` to `urban_twin_*` | **Declined** | Zero functional gain. `from contracts import Observation` appears in nearly every file; the longer name adds noise everywhere. They are `src`-layout packages installed from this repo only, nothing on PyPI shadows them, and renaming touches the frozen layer plus every importer. Documented in README |

---

## 6. Decisions already made

Settled. Do not reopen these mid-week without a team conversation.

| Decision | Rationale |
|---|---|
| **`Geography(POINT, 4326)`, not `Geometry`** | `ST_DWithin(a, b, 25)` means 25 **metres**. With `Geometry` it means 25 degrees and matches half of south India. Verified: two points 110.6 m apart are within 150 but not within 50 |
| **The ORM is the source of truth for schema** | Migration 0001 mirrors `models.py` exactly so `--autogenerate` yields an empty migration. If they drift, every later `make revision` carries spurious ALTERs that somebody eventually applies by accident |
| **`server_default` kept only on `created_at`/`updated_at`** | Those 10 columns declare it in *both* the ORM and the migration, so they agree. All 23 other literal defaults were stripped — Python-side `default=` already covers Core inserts |
| **`FUSABLE_CLASSES`: plain `PEDESTRIAN`/`VEHICLE` are analytics input, not workflow events** | Fusing them handed repair crews work orders with a 30-day SLA for a person walking, and buried real defects in the operator's list. They remain fully available as raw observations to M2 and M3 |
| **`_SAFETY_SEVERITY` table; severity is never fabricated** | `COLLISION → LARGE`, `RASH_DRIVING`/`PEDESTRIAN_RISK`/`NEAR_MISS → MEDIUM` (NEAR_MISS added in the §10 amendment; usually set explicitly from `min_ttc_seconds`, this is the fallback). An explicit reviewable policy, not a fallback — a blanket default rendered a hit-and-run as a small blue dot beside a hairline crack. Infrastructure classes never reach the table; if one does, it raises |
| **Noisy-OR for confidence fusion, not averaging** | `1 − Π(1 − cᵢ)` — the probability at least one detector was right. Two 0.6 sightings give 0.84; averaging would give 0.6 and throw away the corroboration that is the entire premise. Capped at 0.999: we never claim certainty |
| **`--tagged-only` building fetch** | Unfiltered = 146,403 footprints / **31 MB**. Filtered to those declaring `height` or `building:levels` = 7,177 / **1.72 MB**. Same visual read, 18× smaller |
| **`buildings.geojson` is committed, not fetched at runtime** | The twin must render on a fresh clone with no network and no build step. Overpass is rate-limited and will pick your demo to fail on. 1.72 MB once, never changing |
| **27B, 570 and M1 share the Egmore↔Central trunk** | Not decoration. Without a corridor several services traverse, no defect is ever seen by two buses and multi-bus corroboration — the project's central claim — is undemonstrable |
| **Mock GPS jitter is ±3.3 m** | Deliberately under a third of the 25 m fusion cell. Wider noise splits one pothole into several events. Do not widen it casually |
| **Event ids are keyed off the fusion grid cell, not the centroid** | The centroid shifts by a metre whenever a new observation lands, so hashing it minted a fresh uuid on nearly every pass — the map re-created every pin and no event ever accumulated a second bus |
| **`last_seen` is sensor data, never touched by a status change** | It means "when the fleet last *saw* this". Stamping it on a human edit broke under the replay clock: wall-clock time landed *before* a simulated `first_seen` and the validator rejected the response |
| **The replay simulator stays after every module goes real** | You cannot bring six MTC buses to a hackathon |

---

## 7. Standards this project cites

| Standard | What it is | Where it appears |
|---|---|---|
| **AIS-140** | Indian mandate for vehicle tracking units on public service vehicles | `BusPosition` model shape; `Bus.device_serial` column; the telemetry feed the replay simulator stands in for |
| **IRC:82-2015** | Indian Roads Congress code for road maintenance — dimensional severity classes | `Severity` (SMALL/MEDIUM/LARGE) and `contracts.fusion_math.severity_from_dimensions`: >300 mm across or >50 mm deep is LARGE. Cited in the DefectsPanel footer |
| **GTFS** | General Transit Feed Specification — the standard public-transport schema | `Route` model: `route_id`, `stops`, shape polyline |
| **OGC SensorThings API** | Open standard separating raw sensor readings from interpreted features | The `Observation` → `Event` split: observations are cheap, numerous, unverified and never shown to a human; events are fused, corroborated and actionable |
| **DPDP Act 2023 §8** | India's data-protection act — data minimisation | Plates stored as salted SHA-256 only (`config.hash_plate`). There is deliberately **no `plate_text` column** in the `incidents` table; readable text exists only in the live operator dossier, and the IncidentsPanel masks it by default. Enforced by tests |
| **PCI** (ASTM D6433 family) | Pavement Condition Index, 0–100 | `RoadCondition.pci_score`, degraded by observed defect severity |

---

## 8. Test and verification surface

| | Count | Command |
|---|---|---|
| Python tests | **245** | `make test-py` / `.venv/bin/pytest` |
| Frontend tests | **26** (2 files) | `cd apps/command && npm run test -- --run` |
| Smoke checks | **34** | `make smoke` |
| Everything | | `make test` |

### Python tests by file

| File | Tests |
|---|---|
| `packages/contracts/test_contracts.py` | 45 |
| `services/cloud/api/test_module.py` | 38 |
| `services/cloud/consensus/test_module.py` | 29 |
| `services/cloud/intelligence/urban_risk/test_module.py` | 20 |
| `services/cloud/intelligence/whatif/test_module.py` | 16 |
| `services/edge/incidents/test_near_miss.py` | 15 |
| `services/tools/replay/test_module.py` | 15 |
| `services/cloud/intelligence/traffic_analytics/test_module.py` | 14 |
| `services/edge/defects/test_module.py` | 12 |
| `services/edge/incidents/test_module.py` | 12 |
| `services/cloud/intelligence/recommend/test_module.py` | 12 |
| `services/edge/pedestrian/test_module.py` | 11 |
| `scripts/test_pipeline.py` | 6 |

**The API tests run with no infrastructure at all** — that is deliberate, and it is what
keeps the "degrade rather than 500" property honest. The whole suite passes with postgres
up *and* with postgres stopped.

**`scripts/test_pipeline.py`** is the one that matters most: it wires the simulator
straight into the API's `LiveState` and `FusionLoop` with no broker and no database, and
asserts the actual story happens — buses move, detections accumulate, events form,
corroboration from multiple buses escalates them, the WebSocket sees it, and a human
decision is never overwritten by the next fusion pass. It catches "every module passes its
own tests but nothing works together", which is the classic way a six-person hackathon
project dies on day six.

### `make smoke` covers

postgres connectivity · PostGIS extension · all 9 tables · geography columns registered ·
GIST indexes · seed data loaded · redis PING + SET/GET · MQTT connect + publish→subscribe
roundtrip · all 8 factories returning a Protocol-conforming implementation · 12 HTTP
endpoints · WebSocket accept · frontend deps installed · buildings cached.

### `make mine`

Runs **only your module's tests**, detecting who you are from your git branch
(`m3-fusion` → `m3`), or from `MEMBER=m3`.

| Member | Runs |
|---|---|
| m1 | `services/edge/defects` |
| m2 | `services/cloud/intelligence/traffic_analytics`, `services/cloud/intelligence/whatif`, `services/cloud/intelligence/recommend` |
| m3 | `services/edge/pedestrian`, `services/cloud/consensus`, `services/cloud/intelligence/urban_risk` |
| m4 | `services/edge/incidents` (includes `near_miss.py` + its tests) |
| m5 | `services/cloud/api`, `packages/db`, `scripts` |
| m6 | `packages/contracts` (+ `npm run test` in `apps/command`) |

### Quality gates

```bash
make lint        # ruff, line length 100
make typecheck   # mypy strict on contracts + db + citydata
make fmt         # ruff format + autofix
```

CI (`.github/workflows/ci.yml`) runs lint, strict typecheck, migrations against a real
PostGIS service, seed, the full pytest suite, and both frontend builds — plus a guard that
fails if any `USE_REAL_*` is `true` in `.env.example`.

---

## 9. Build timeline

This repo was produced by four prompt passes: **scaffold → verify → fix → ship.**

| Phase | What happened |
|---|---|
| **Scaffold** | Whole repo generated in one pass: contracts, six modules with mocks and stubs, API, DB, both frontends, tooling, docs |
| **Verify** | 34-point audit against live infrastructure — real containers, PostGIS queries, `mosquitto_sub` roundtrips, headless Chrome over CDP for the UI. Report produced, no fixes applied |
| **Fix** | 12 fixes applied P0→P4, each re-verified. 2 items declined with reasons. 7 findings deferred and annotated in code |
| **Ship** | History rewritten into 8 logical commits, 6 member branches created, pushed |

```
f02e22b  chore: project scaffold, docker compose, tooling         13 files,   +758
8095e61  feat(contracts): frozen event schema, protocols, math    10 files,  +1992
4735374  feat(db): postgis models and initial migration           12 files,  +1020
d9db90f  feat(api): rest endpoints, websocket, mqtt ingest        29 files,  +3134
1b76812  feat(perception): mock implementations for all 4 tracks  25 files,  +1969
564d2b2  feat(analytics): traffic and whatif mocks                13 files,   +800
73fce5e  feat(frontend): command centre, 3d twin, field app       53 files, +13294
cdf0c40  docs: readme, contributing, per-member checklists        12 files,  +1418
```

Branches `main`, `m1-defects`, `m2-traffic`, `m3-fusion`, `m4-incidents`, `m5-platform`,
`m6-frontend` all start from `cdf0c40`.

---

## 10. AI Intelligence Layer — contracts amendment (v1.1.0)

**One approved, one-time unfreeze.** `packages/contracts` moved from `1.0.0` to `1.1.0`
in a single batch, then was **re-frozen as of this commit** — the same rules in §3
apply again from here: an amendment needs agreement from every owner it touches, and it
does not happen twice in a sprint.

### What changed, and why it stayed additive

Nothing existing was renamed, removed, or had a signature changed. Every addition is one
of the two cheap kinds §3 already permits — a new enum member or a new optional field —
plus two entirely new Protocols, which is additive to the *set* of module boundaries, not
a change to any existing one:

| Added | Where |
|---|---|
| `DetectionClass.NEAR_MISS` | `enums.py` — also joined `FUSABLE_CLASSES` and `SAFETY_CLASSES` |
| `RiskBand`, `RecommendationType` | `enums.py` — new StrEnums, not reuses of `RiskLevel` (see its docstring for why they stay separate) |
| `RoadCondition.urban_risk_score` / `.risk_band` / `.near_miss_count_7d` | `models.py` — all optional/defaulted, existing constructions still validate |
| `AnalyticsSummary.critical_risk_roads` / `.open_recommendations` / `.near_misses_7d` | `models.py` — same, defaulted to 0 |
| `UrbanRiskScore`, `InfrastructureRecommendation`, `NearMissEvent` | `models.py` — 3 new wire models (14 → **17** total) |
| `RiskContext` | `interfaces.py` — a plain dataclass, deliberately not a pydantic model: it never crosses MQTT or HTTP, only passed in-process from the API to the two new Protocols |
| `RiskScorer`, `RecommendationEngine` | `interfaces.py` — 2 new Protocols (6 → **8** total) |
| `USE_REAL_RISK`, `USE_REAL_RECOMMEND` | `.env.example` — both `false`, same CI guard as the other six |

### The two new modules

`services/cloud/intelligence/urban_risk/` (M3) and `services/cloud/intelligence/recommend/` (M2) follow the identical six-file shape
every other module uses. Neither needed a database migration or a new MQTT topic — both
are computed per-request from `LiveState` plus static city reference data, the same way
`TrafficAnalyzer.analyze()` and `WhatIfEngine.simulate()` already were. `RiskContext` is
built in exactly one place (`services/cloud/api/intel_context.py`) so `/api/roads`,
`/api/roads/{id}/risk` and the KPI strip's `critical_risk_roads` can never drift the way
`open_events` once did (§4's fix log, F9) — see that file's docstring.

`services/cloud/intelligence/urban_risk/mock.py`'s weighted index is **not a placeholder** — it is the intended v1,
same status as `services/cloud/consensus/mock.py`'s clustering. The learned upgrade in `impl.py`
must keep the same explainability guarantee (`components` sum to `score`, `explanation`
never empty) or it is not an upgrade, only a different set of numbers.

### Near-miss: zero new models

The project's most novel feature, and the one most likely to be misbuilt by reaching for
a new detector. It requires **ZERO new models** — the full reasoning is the TODO block at
the top of `services/edge/incidents/impl.py`, but the one-line version: reuse the
existing vehicle and pedestrian ByteTrack output, project both onto the ground plane with
the calibration M1 already derives, and take a geometric time-to-collision. `TTC < 0.5s`
is `LARGE`, `< 1.0s` is `MEDIUM`, otherwise `SMALL` — not the IRC:82-2015 table, which
governs surface distress dimensions and does not apply here.

The mock (`near_miss.py`) scripts three junctions, one per route, and emits near-misses
two ways: as an `Observation` with `detection_class=NEAR_MISS` that flows through the
*normal fusion path* (so a repeated near-miss escalates through the workflow ladder like
any other event), and as the richer `NearMissEvent` (TTC, track ids, closing speed) that
`GET /api/near-misses` serves directly — independent of the replay simulator having ever
run, the same "ask the mock for the scripted scene" pattern `routers/incidents.py` already
used for the hit-and-run dossier.

### New endpoints and panel

Four new routes in `routers/intelligence.py` (`GET /roads/{id}/risk`,
`GET /recommendations`, `GET /near-misses`, `GET /junctions/dangerous`) — 15 → **19** HTTP
routes. One new frontend panel, `IntelligencePanel.tsx`, registered in `Sidebar.tsx`
exactly like the other five. Two new deck.gl layers in `MapCanvas.tsx`: near-miss markers
(a distinct warning-diamond icon, never confusable with an event dot) and a toggleable
risk-band layer at each road segment's centre.

---

**Now read README.md § your member block, then run `make dev`.**

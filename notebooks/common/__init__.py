"""Shared helpers for the ML training notebooks. Not part of the ``urban_twin``
package or CI — imported only from inside notebooks/, after
``common.colab.setup_environment()`` has put the repo root on ``sys.path``.
"""
